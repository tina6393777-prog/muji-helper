# MUJI 客服查詢小幫手（muji-helper）改版紀錄

日期：2026-08-19
原網站：https://tina6393777-prog.github.io/muji-helper/
GitHub repo：tina6393777-prog/muji-helper（public）

## ⚠️ 網頁本體 index.html 沒有存進 Project（技術限制）
原本想把改版完成的 index.html 存一份進 Project，方便以後開新對話直接接續修改，但這個檔案因為內嵌了大量圖片（SOP流程圖、QA圖片等，用 Base64 編碼），實際換算成文字量高達約 235 萬字符單位，超過 Project 知識庫上限（200 萬），存進去會直接把整個 Project 塞爆，所以已經刪除、沒有存進去。quiz-muji.html（測驗工具，見下方）字數較小，沒有這個問題。

**目前 index.html 的正本只有兩個地方：**
1. 使用者的 GitHub repo（tina6393777-prog/muji-helper）— 這是網站實際運作的正本
2. 這次對話裡用「下載檔案」方式交付的版本 — 只在這個對話串看得到

之後如果要在新對話繼續修改這個網站，建議直接把 GitHub 上的 index.html 下載後重新上傳給 Claude，或是告訴 Claude 去讀取 GitHub repo 的內容（Claude 可以用網路工具讀取 public repo 的檔案）。

## repo 檔案現況（改版後）
- `index.html`：GitHub Pages 實際讀取的檔案，已改版完成，使用者已上傳覆蓋
- `muji-helper.html`：舊版備份檔，內容已同步更新成跟 index.html 一樣（純備份，GitHub Pages 不會讀它）
- `quiz-muji.html`：客服知識測驗工具。2026-08-19 更新為 31題/題庫116題；**2026-08-28 再更新**：移除 9 題重複是非→題庫 107 題、每份加是非題上限 5 題、畫面改 MUJI 深棗紅風（#7B1A1F）。詳見下方兩則「測驗工具更新」
- `01_MUJI_QA知識庫_ECP.xlsx`、`03_MUJI_常見問題.xlsx`：repo 裡原本的舊備份 Excel，網頁不會讀取，只是存檔用。使用者已決定改用這次的 6 份新 Excel 取代備份（見下方檔名建議）
- 新的 6 份備份 Excel（已交付給使用者，待上傳到 repo，可取代舊的 01/03）：
  - `03.MUJI_衣著尺寸保養方式.xlsx`
  - `04.MUJI_常見問題.xlsx`
  - `05.MUJI_門市資訊.xlsx`
  - `06.MUJI_DC馬達風扇全台維修據點.xlsx`
  - `MUJI_文字客服回覆範本.xlsx`
  - `MUJI常用網頁帳密.xlsx`
- `catalogs/25AWfurniture.pdf`、`catalogs/25AWstorage.pdf`：**不需要上傳**，網站已改成只連結官方電子型錄，不再使用這兩個檔案

## 這次改版做了什麼

用使用者提供的 8 份新資料，把原本 8 個頁籤的網站改版成 10 個頁籤，並修好一個原本就存在的 bug。

### 資料來源對照
- `03.MUJI_衣著尺寸保養方式.xlsx` → 尺寸查詢頁籤（29 組對照表，含女裝/男裝/LABO/童裝/內衣/鞋類）+ 洗滌與保養頁籤新增的「素材特性」「紗線織法小知識」
- `04.MUJI_常見問題.xlsx`（17 個分頁）→ 知識庫頁籤（327 筆 QA，17 個分類 chip），並抽取當中 12 張內嵌圖片（MUJI passport、家具配送、禮盒包裝）對應到正確題目
- `05.MUJI_門市資訊.xlsx` → 門市服務頁籤（80 家門市，含完整服務項目），另加官網門市總覽連結 https://shop.muji.tw/v2/Shop/StoreList/40820
- `06.MUJI_DC馬達風扇全台維修據點.xlsx` → DC風扇維修頁籤（26 個服務中心，內容與舊版一致，沿用舊版的地區分類）
- `14.MUJI_客服案件處理SOP手冊.docx` → 新增「客服SOP」頁籤，21 個流程、5 大類別，每個流程都保留原始流程圖（原圖已內嵌）
- `MUJI_文字客服回覆範本.xlsx` → 回覆範本頁籤。原本 143 則，後來比對發現舊網站有 15 則新版沒帶到（主要是「開頭語」整個分類 6 則、幾筆商品規格參考、出貨/退款補充），已經補回，現在共 **158 則**
- `MUJI常用網頁帳密.xlsx` → 新增「常用網頁帳密」頁籤，加上警告標語提醒使用者確認 repo 是否為 Private
- `25AWfurniture.pdf` + `25AWstorage.pdf` → 一開始有做成「型錄快速索引」，但考量每季換新型錄的維護成本，最後拿掉，改成只保留無印良品官方電子型錄連結（https://shop.muji.tw/muji-journal/pdf/）
- 說明書頁籤：MANUALS 相關程式碼已整個移除（修好舊版會報錯的 bug），改成「前往無印良品官網『商品說明書』總覽」按鈕（https://www.muji.tw/muji-journal/team-pdf-manual/）

## 測驗工具更新（quiz-muji.html，2026-08-19）
使用者要求把測驗工具跟著這次改版的新內容一起更新。做法：
- 因為原本quiz-muji.html不在這次上傳的檔案裡，改用網路工具去讀取使用者GitHub repo裡現有的 quiz-muji.html，用「逐字複製、不摘要」的方式一段一段抓出完整的原始題庫（100題）、CSS、HTML結構、JS邏輯（含EmailJS寄信、Google試算表寫入的設定），確保原本內容不失真（有做重複比對，確認100題無缺漏無重複）。
- 新增 2 個分類、共16題新題目，都是根據這次改版新增的資料（客服SOP手冊、尺寸/材質保養資料）出題，確保正確性：
  - 「客服SOP流程知識」8題：出自14.MUJI_客服案件處理SOP手冊.docx的21個流程（例如颱風配送延遲、DC拋單庫缺、客服室分派入口等）
  - 「尺寸與洗滌保養知識」8題：出自03.MUJI_衣著尺寸保養方式.xlsx（材質特性、洗滌保養方式、尺寸對照表邏輯）
- 題庫從 100題→**116題**，每次測驗抽題數從 25題→**31題**，及格分數仍是85分，其他所有功能（EmailJS自動寄成績信、Google試算表紀錄、計分邏輯、UI）完全保留不變。
- 已用 Playwright 實際跑過完整測驗流程（含多次隨機抽題）、node --check驗證JS語法，確認題庫、抽題邏輯、計分、寄信/寫入試算表的呼叫都正常，無錯誤。

## 測驗工具再次更新（quiz-muji.html，2026-08-28）
使用者要求三件事：**是非題比重減量、避免重複題、測驗畫面改成 MUJI 風格**。做法：
- 先用網路工具抓 GitHub repo 現有的 quiz-muji.html（116題）逐字比對，統計題型分布（是非 34／單選 71／複選 11，是非佔 29.3%），並盤點重複題。
- **移除 9 題重複是非**：都是「是非題在重複問單選題已考過的同一件事」——門市7天、蝦皮跟官網一樣、A類結案、退款金額小可自答、案件編號手動輸入、口頭答應退款、社群發言、生日禮多次、發卡銀行台新。題庫 **116 → 107 題**，是非佔比降到 **23.4%**。
- **加是非題上限（每份最多 5 題）**：抽題數維持 **31 題**、分類抽題數 QUIZ_PLAN 不變（11 類各抽固定題數）；抽完後若是非超過 5 題，自動以「同分類、未抽到的單選／複選」替換（同分類替換以維持 11 分類涵蓋與各類題數）。實測每份大多落在 5 題是非（約 16%）。
- **畫面全面改 MUJI 深棗紅風**：由原本「深藍底＋綠按鈕」改為 **深棗紅 #7B1A1F ＋ 米色底 #F4F1EA ＋ 亞麻邊 #CFC8B8 ＋ 墨灰字 #3A3632**。做法：重新定義 :root 變數值（--green 系列→棗紅、--bg→米色等）自動帶動所有 var() 用色，再對硬寫色碼全域替換；題型標籤調成三色分明（是非=灰藍／單選=沉綠／複選=玫瑰紅），答對(沉綠)／答錯(柔紅)語意色也改為 MUJI 沉色但仍清楚可辨。主色是 **#7B1A1F 深棗紅**（月報 PPT 那個色），刻意不用查詢網站的 #B81C22。
- **完全不變**：及格 85 分、EmailJS 自動寄成績信、Google 試算表寫入、計分邏輯全部原樣保留。
- **驗證**：node --check 過 JS 語法；模擬抽題 3000 次確認每份 31 題、是非≤5、11 分類每份都抽到、單份不重複；Playwright 實際渲染開始頁／載入頁／答題頁截圖確認 MUJI 風格與答對答錯配色正常。
  - 註：測試環境連不到 emailjs 的 CDN（403），截圖時用臨時檔把 emailjs stub 掉才跑得動；正式交付檔的 emailjs 設定未動，線上 GitHub Pages（有網路）會正常寄信。

## 資料缺口分析（2026-08-19 事後盤點）
使用者問「還缺什麼」，實際檢查目前六份資料後，發現的缺口，依重要性排序（後續使用者已回覆部分項目，見狀態欄）：
1. **客訴分級與後送標準偏薄**（狀態：**已釐清，不是缺口**）——原本以為知識庫/回覆範本裡這塊資料很少，後來查 quiz-muji.html 現有題庫才發現，其實「客訴分級與後送SOP」這塊知識已經存在（只是放在測驗工具裡，沒有放進查詢網站的知識庫）。
2. **緊急聯絡人名單（假日/夜間）**（狀態：**已確認，不是缺口**）：使用者確認資料其實已經有，只是這次先選擇不放上網站（跟帳密類似的考量），之後要放的話可以再處理。
3. **電話應答話術可能有漏**：回覆範本 158 則的管道只分「官網」（62）、「蝦皮」（75）、「通用」（21），沒看到專屬的「電話應答」話術庫；但帳密頁籤裡有「宏華話務平台」，代表電話應該也是主要服務管道，建議跟PM/廠商確認是否漏帶電話專用範本。（尚未處理）
4. **商品說明書條碼對照表**（已知缺口，見下方待辦）。（尚未處理）
5. **KPI／品質稽核標準**（狀態：**已確認，不是缺口**）：使用者說明這塊是另外做的報表，不需要放進這個查詢系統。
6. **quiz-muji.html 測驗題庫沒跟著更新**（狀態：**已完成**，見上方「測驗工具更新」）。

## 重要待辦／提醒
1. **帳密頁籤是公開風險點**：使用者已選擇把帳密留在網站上，但要求把 GitHub repo 設為 Private。務必提醒/確認這件事，否則帳密會公開在網路上。（尚未確認使用者是否已實際去 GitHub 設定）
2. 「常用聯絡人」分頁在原始 Excel 裡只有標題列，沒有實際聯絡資訊；使用者後續確認緊急聯絡人資料其實已經有，只是選擇先不放上網站，之後有需要再處理。
3. 說明書頁籤目前沒有商品條碼清單，只能先靠官網連結查詢；之後有整理好的條碼／PDF對照表可以再補上讓站內搜尋恢復。
4. 客訴分級與後送SOP的知識其實已經在quiz-muji.html的題庫裡，之後如果想把這塊也加進查詢網站的知識庫頁籤，可以考慮把測驗題庫裡的這些內容轉成QA格式補進去。
5. 電話應答話術資料偏薄，建議列入下次跟PM/廠商要資料的清單。

## 技術細節
- index.html：單一 HTML 檔案（CSS/JS 全內嵌），沿用原站的 MUJI 紅色（#B81C22）風格與既有的搜尋/分類/展開互動模式。檔案大小約 2.8MB（主要是內嵌圖片：SOP 流程圖 21 張＋QA 圖片 12 張＋量測示意圖 2 張）。因為含大量 Base64 圖片，這個檔案**不適合存進 Project 知識庫**（會超過容量上限），只能放在 GitHub repo 或用對話下載。
- quiz-muji.html：單一 HTML 檔案，約 63KB（2026-08-28 版），用 EmailJS（CDN: cdn.jsdelivr.net/npm/@emailjs/browser）寄成績信給主管信箱、用 Google Apps Script webhook 寫入 Google 試算表，這兩個外部服務的設定值（EMAILJS_SERVICE_ID等、SHEET_URL）都完整保留原樣。
- 已用 Playwright 對 index.html 的10個頁籤、quiz-muji.html的完整測驗流程分別測試過，並用 node --check 驗證JS語法，皆無錯誤。
- 建置用的 Python 腳本與中繼 JSON 存在這次 session 的 `/home/claude/muji-build/`，下次若要再更新資料，可以參考這個資料夾的處理邏輯（session 結束後不會保留，如需重建請重新走一次 Excel/Word/PDF 解析流程）。
